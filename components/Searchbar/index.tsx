import { Input } from "@chakra-ui/react";

const SearchBar = () => {
  return (
    <Input
      my="6"
      border="1px"
      borderColor="gray.900"
      color="white"
      placeholder="RECHERCHER"
    />
  );
};

export default SearchBar;
